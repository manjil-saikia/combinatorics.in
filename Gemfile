source "https://rubygems.org"

# Matches what GitHub Pages actually runs, so local previews cannot drift.
gem "github-pages", group: :jekyll_plugins

# Ruby 3 no longer ships webrick, which `jekyll serve` needs.
gem "webrick", "~> 1.8"
